/**
 * 用户 / 机构配置文件
 * 修改后刷新页面即可生效（本地设置里改的姓名会优先覆盖这里）
 */
window.GESP_CONFIG = {
  // 显示在首页、宠物祝福中的名字
  userName: "同学",

  // 机构名称（可选，显示在标题旁）
  orgName: "",

  // 宠物祝福概率 0~1（每次进入首页）
  petBlessChance: 0.45,

  // 默认题库
  defaultBank: "printf-scanf"
};
